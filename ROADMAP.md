# ESP8266 BME280 → Google Sheets 移行ロードマップ

## 目的

ESP8266（ESPr Developer）とBME280で測定している温度・気圧・湿度を、
AmbientからGoogleスプレッドシートへ移行する。

個人利用・趣味のプロジェクトとして、無料で維持できる構成を優先する。

## 既存スケッチの原典

既存の動作確認済みスケッチは、次の公開リポジトリで管理されている。

https://github.com/tahosook/sketch_ambidata

移行先では、このリポジトリのBME280読み取り処理、Wi-Fi接続、ディープ
スリープ動作をベースラインとして扱う。Ambientへの送信部分だけを段階的に
置き換える。既存リポジトリのMITライセンスと著作権表示は引き継ぐ。

## 合意済みの方針

- GitHubリポジトリはPublicで運用する。
- Wi-Fiパスワード、APIトークン、GASの認証トークンは公開しない。
- データ欠損は許容する。未送信データの永続キューは作らない。
- APIバージョンは最初から `1` とする。
- 単位は `temp: °C`, `press: hPa`, `hum: %` に固定する。
- 日時はGAS側で生成し、タイムゾーンは `Asia/Tokyo` とする。
- 実機書き込みとGASデプロイはユーザーが確認してから行う。
- 安定版はGitタグで管理する。
- 追加サービスや有料インフラは使用しない。

## 開発フロー

```text
Issue作成
  ↓
codex/* ブランチで小さく実装
  ↓
ローカル検証・差分確認
  ↓
Pull Request
  ↓
人間による確認
  ↓
mainへマージ
  ↓
必要に応じてGASデプロイ・実機書き込み
```

## Phase 0：GitHubと開発基盤の整備

### 作業

- Public GitHubリポジトリを作成する。
- `main`を安定ブランチとして扱う。
- `AGENTS.md`、`README.md`、このロードマップを登録する。
- `.gitignore`を設定する。
- 既存スケッチから秘密情報を除いたベースラインを移す。
- `sketch_ambidata`のMITライセンスと著作権表示を引き継ぐ。
- `secrets.example.h`を作成する。
- GitHub IssueとPull Requestを作業単位にする。

### 完了条件

- 公開しても秘密情報が含まれない。
- 既存スケッチの出所と移行対象がリポジトリ内で確認できる。
- Codexが`AGENTS.md`を読めば、プロジェクトの前提を理解できる。

## Phase 1：API仕様の確定

### POSTデータ

```json
{
  "api_version": 1,
  "token": "local-secret",
  "temp": 24.5,
  "press": 1012.3,
  "hum": 55.8
}
```

### スプレッドシート列

```text
日時 | temp | press | hum
```

### 完了条件

- JSON項目名と単位が文書化されている。
- 正常値、欠損値、異常値、不正トークンの扱いが決まっている。
- 成功時・失敗時のHTTPレスポンスが決まっている。

## Phase 2：GAS APIの実装

### 作業

- `doPost(e)`を実装する。
- JSONを解析する。
- APIバージョンを確認する。
- トークンを検証する。
- 数値と許容範囲を検証する。
- `Asia/Tokyo`の日時とともにシートへ追記する。
- JSONレスポンスを返す。

### 検証

- curl等で正常POSTを送る。
- 正常な1行が追加されることを確認する。
- 不正リクエストが拒否されることを確認する。

## Phase 3：Arduino送信処理の実装

### 作業

- BME280の既存処理を維持する。
- Ambient送信処理をGAS送信処理へ置き換える。
- `WiFiClientSecure`と`HTTPClient`を利用する。
- `ArduinoJson`でPOST本文を生成する。
- GASのリダイレクトに対応する。
- HTTPステータスとレスポンスをシリアル出力する。
- 送信後に既存の5分ディープスリープへ戻る。

### 完了条件

- ESP8266用スケッチがコンパイルできる。
- BME280の値が取得できる。
- GASから成功レスポンスを受け取れる。
- スプレッドシートに値が追加される。

## Phase 4：通信・実機検証

### 検証項目

- Wi-Fi接続
- BME280読み取り
- HTTPS POST
- HTTP 200の成功判定
- GASリダイレクト
- スプレッドシート追記
- 5分後のディープスリープ復帰
- Wi-Fi切断時のタイムアウト
- GASエラー時のログ出力

### 記録

検証結果は`docs/test-results/`へ記録する。パスワードやトークンは記録しない。

## Phase 5：最小限の安定化

### 作業

- Wi-Fi接続タイムアウトを追加する。
- HTTPS通信タイムアウトを追加する。
- 送信失敗時に最大2〜3回だけ再試行する。
- 失敗しても無限ループせず、次回測定へ進む。
- シリアルログの形式を固定する。

### 方針

データ欠損は許容するため、LittleFS等による未送信データの保存は実装しない。

## Phase 6：レビューと安定版化

### レビュー項目

- 公開リポジトリに秘密情報がない。
- APIの項目名と単位が一致している。
- Wi-FiやHTTPS処理が無限待機しない。
- リダイレクト回数に上限がある。
- 既存のセンサー処理を意図せず壊していない。
- 実機テスト結果が記録されている。

動作確認済みのファームウェアをGitタグで固定する。

例：

```text
v0.1.0-gas-api
v0.2.0-first-device-upload
v1.0.0-stable
```

## GitHub Actionsの扱い

無料枠を優先し、最初は以下の軽量なチェックだけを自動化する。

- `git diff --check`
- 秘密情報の混入確認
- Arduinoスケッチのコンパイル
- 必要なライブラリの確認

GASの自動デプロイは初期段階では行わず、仕様と実機動作が安定してから検討する。
